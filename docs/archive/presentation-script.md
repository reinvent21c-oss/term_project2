# 고객 리뷰 감정 분석 대시보드 (CLI) — 발표 스크립트

> 미션 명세서(`introduction.md`)의 목차 1~8을 그대로 따라갑니다.
> 심사자가 명세서를 손에 들고 있어도 항목을 잃지 않게 하기 위해서입니다.
>
> `>` 로 들여쓴 문단이 **실제로 말할 대사**입니다. 그대로 읽어도 되고, 키워드만 뽑아 쓰셔도 됩니다.

---

## 발표 구성

| # | 슬라이드 | 명세 대응 | 발표 | 시간 |
|---|---|---|---|---|
| S0 | 표지 | — | 영휘 | 0:15 |
| S1 | 왜 만들었나 | 1 | 영휘 | 1:00 |
| S2 | 네 줄이면 리포트까지 | 2 | 영휘 | 1:00 |
| S3 | 우리가 답할 수 있게 된 것 | 3 | 영휘 | 0:40 |
| S4 | 아키텍처와 분업 | — | 영휘 | 1:30 |
| S5 | CLI 설계 | 4.1 | 영휘 | 0:50 |
| S6 | 수집과 정제 | 4.2 · 4.3 | 세인 | 1:50 |
| S7 | AI 감정 분석 | 4.4 | 민규 | 1:30 |
| S8 | 키워드·요약 추출 | 4.5 | 민규 | 1:00 |
| S9 | 조회와 검색 | 4.6 | 영휘 | 0:50 |
| S10 | 대시보드 시각화 | 4.7 | 영휘 | 1:30 |
| S11 | 리포트 생성 | 4.8 | 민규 | 1:00 |
| S12 | 내보내기 | 4.9 | 영휘 | 0:30 |
| S13 | 설정·로깅·저장·구조·샘플 | 4.10~4.13 | 영휘 | 1:00 |
| S14 | 보너스 과제 | 5 | 영휘 | 1:00 |
| S15 | 개발 환경과 제약 | 6 · 7 | 영휘 | 0:30 |
| S16 | 라이브 데모 | 8 | 3인 | 3:00 |
| S17 | 우리는 이걸 어떻게 믿나 | — | 영휘 | 1:00 |
| S18 | 남은 것과 배운 것 | — | 3인 | 1:00 |

합계 약 **20분** + Q&A. 시간이 모자라면 S12·S13을 한 장으로 합치고 S16 데모를 8.4·8.6만 남기세요.

---

## S0. 표지 · 영휘 · 0:15

**화면**

```
고객 리뷰 감정 분석 대시보드 (CLI)

7팀   A 송영휘 · B 이세인 · C 김민규
Python 9,199줄 · CLI 12개 명령 · SQLite 5테이블 · 차트 7종
```

> 안녕하세요, 7팀입니다. 고객 리뷰를 AI로 분석해서 대시보드까지 만드는 CLI 도구를 만들었습니다.
> 세 명이 폴더를 하나씩 나눠 맡았고, 오늘은 명세서 순서 그대로 따라가면서 보여드리겠습니다.

---

## 1. 미션 소개

### S1. 왜 만들었나 · 영휘 · 1:00

**화면**

```
리뷰 1,000건  →  사람이 읽으면 며칠
              →  AI로 분류만 하면 몇 분,  그런데 그래서 뭘 하지?

우리가 만든 건 "분류기"가 아니라 "그 다음"입니다.

  분류    →  긍정 57.6% / 중립 38.4% / 부정 4.0%
  추이    →  2025-05 부정 비율 급증 (0% → 14%)
  원인    →  '유분 증가' 8건 · 평균 별점 2.75 (전체 3.65)
  행동    →  개선 우선순위 1위
```

> 리뷰 천 건을 사람이 읽으면 며칠이 걸립니다. AI를 쓰면 몇 분이면 끝나죠.
>
> 그런데 저희가 처음에 만들어 보니, 긍정 몇 퍼센트, 부정 몇 퍼센트까지만 나오면
> 보고서에는 예뻐 보이는데 정작 **"그래서 뭘 고치죠?"** 라는 질문에는 답을 못 하더라고요.
>
> 그래서 목표를 하나 더 잡았습니다. 분류에서 멈추지 않고, 언제 나빠졌는지, 어디에 몰려 있는지,
> 뭐부터 고쳐야 하는지까지 숫자로 내는 것. 오른쪽 네 줄이 오늘 보여드릴 흐름입니다.
>
> 실제로 저희 데이터 99건에서, 2025년 5월에 부정 비율이 0%에서 14%로 튀는 구간을 도구가 스스로 잡아냈고,
> 개선 우선순위 1위로 '유분 증가'를 뽑았습니다. 이 키워드가 붙은 리뷰 8건의 평균 별점이 2.75점,
> 전체 평균 3.65점보다 0.9점 낮습니다.

**포인트** — 여기서 숫자를 던져놓고 끝내지 말고 "이 숫자가 어떻게 나오는지 지금부터 보여드리겠습니다"로 넘기세요.

---

## 2. 최종 결과물

### S2. 네 줄이면 리포트까지 · 영휘 · 1:00

**화면**

```bash
python main.py import --file source/input/cosmetics_reviews_100.csv   # 2.1 수집·저장
python main.py analyze --unanalyzed --limit 25                        # 2.2 감정 분석
python main.py extract                                                # 2.3 키워드·요약
python main.py dashboard                                              # 2.5 차트 + 리포트
```

| 명세 | 결과물 |
|---|---|
| 2.1 수집·저장 | CSV·Excel → `raw_reviews` / `reviews` 2단 저장, 중복 `skip`·`upsert` |
| 2.2 감정 분석 | 감정 3종 + 확신도 0.0~1.0, `--all` `--id` `--unanalyzed` |
| 2.3 키워드·요약 | 긍정/부정 키워드 · 전체 요약 · 개선 제안 |
| 2.4 조회·검색 | `list` 필터·정렬·페이지네이션, `show` 상세 |
| 2.5 대시보드·리포트 | PNG 7종 + Markdown 리포트 + CSV/JSONL/XLSX |

> 최종 결과물은 CLI 애플리케이션 하나입니다. 명령어는 12개인데, 실제로 처음부터 끝까지 가는 건 이 네 줄입니다.
>
> 원본 CSV를 넣으면 정제해서 DB에 쌓고, 감정을 분석하고, 키워드를 뽑고, 차트 일곱 장과 리포트가 나옵니다.
> 명세서 2.1부터 2.5까지가 이 네 줄 안에 다 들어 있습니다.
>
> 그리고 이 중에 실제로 API를 부르는 건 가운데 두 줄뿐입니다. 키가 없어도 나머지 열 개 명령은 다 돕니다.
> 이건 뒤에서 다시 말씀드릴 텐데, 저희가 의도적으로 그렇게 잘랐습니다.

---

## 3. 과제 목표

### S3. 우리가 답할 수 있게 된 것 · 영휘 · 0:40

**화면**

```
명세 3장의 네 가지 목표  →  발표에서 답하는 슬라이드

① 데이터를 읽고 정제하는 과정          →  S6  (세인)
② AI로 감정 분석하고 구조화해 저장     →  S7  (민규)
③ 집계하고 matplotlib으로 차트 생성    →  S10 (영휘)
④ 비즈니스 관점 해석과 인사이트 도출   →  S8 · S11 · S14
```

> 명세서 3장에 "이걸 스스로 설명할 수 있어야 한다"는 항목이 네 개 있습니다.
> 저희 발표는 그 네 개에 각각 슬라이드를 하나씩 배정했습니다.
> ①번은 세인님이, ②번은 민규님이, ③번은 제가, ④번은 셋이 나눠서 말씀드리겠습니다.

---

## S4. 아키텍처와 분업 · 영휘 · 1:30

> 명세에는 없지만 **팀 프로젝트라서 반드시 들어가야 하는 장**입니다. 심사자가 가장 많이 묻는 지점입니다.

**화면**

```
        ┌─────────────┐
        │  A  chart/  │   DB · CLI · 집계 · 차트 · export · 통합
        └──────┬──────┘
               │  bridge.py 한 곳에서만 호출
       ┌───────┴───────┐
       ▼               ▼
┌─────────────┐ ┌─────────────┐
│  B source/  │ │  C prompt/  │
│  수집·정제   │ │  AI·리포트   │
└─────────────┘ └─────────────┘

B와 C는 A를 import 하지 않는다.
```

| 담당 | 폴더 | 범위 | 규모 |
|---|---|---|---|
| A 영휘 | `chart/` | DB · CLI · 집계 · 차트 · export · 통합 | 8,224줄 |
| B 세인 | `source/` | 수집 · 정제 | 229줄 |
| C 민규 | `prompt/` | AI 분석 · 인사이트 · 리포트 | 693줄 |

> 세 명이 같은 파일을 건드리면 머지 충돌로 시간을 다 씁니다. 그래서 **폴더를 하나씩 나눠 갖고,
> 자기 폴더만 수정한다**로 시작했습니다.
>
> 중요한 건 화살표 방향입니다. B와 C는 A를 import 하지 않습니다. 한 방향입니다.
> 그래서 세인님은 제 코드 없이 `source/`만으로 개발하고 테스트할 수 있었고, 민규님도 마찬가지였습니다.
>
> 그리고 A가 B·C를 부르는 자리도 한 곳으로 몰았습니다. `bridge.py` 파일 하나입니다.
> 상대 폴더 구조가 바뀌면 이 파일 하나만 고치면 됩니다.
>
> 하나 더 있습니다. 저희는 **숫자를 한 곳에서만 셉니다.**
> 차트가 자기 손으로 세고 리포트가 또 세면, 같은 데이터인데 차트는 89건 리포트는 90건이 되는 일이 반드시 생깁니다.
> `calculate_stats()` 함수 하나가 여섯 칸짜리 결과를 만들고, 차트는 `chart_data` 칸만,
> 리포트는 `summary`·`quality`·`top_n` 칸만 봅니다.

**예상 질문 대비** — "왜 `__init__.py`를 안 넣었나요?"
> `prompt/`와 `source/src/`에는 `__init__.py`가 없습니다. 제가 하나 넣는 순간 그 폴더는 더 이상 그 사람만의 것이 아니게 됩니다.
> 그래서 패키지 import 대신 파일 경로로 모듈을 직접 읽는 로더를 제 쪽에 뒀고, 이 경계를 감시하는 테스트도 있습니다.

---

## 4. 기능 요구 사항

### S5. 4.1 CLI 설계 · 영휘 · 0:50

**화면**

```
필수 10개   import  add  clean  analyze  extract  list  show  stats  dashboard  export
추가  2개   review (AI 검수)   status (현황)

argparse 서브파서 + 공통 부모 파서(--verbose/--quiet 어디에 붙여도 됨)

종료 코드   0 성공 · 1 실패 · 130 중단 · 141 파이프 끊김
            2 일부 실패  ← analyze · review load · review score
```

> 명세 4.1이 요구한 필수 서브커맨드 열 개, 전부 있습니다. 여기에 두 개를 더 만들었습니다.
> `review`는 AI 판정을 사람이 검수하는 명령이고, `status`는 지금 DB에 뭐가 얼마나 있는지 보는 명령입니다.
>
> `argparse`는 서브파서로 짰고, 공통 옵션은 부모 파서로 빼서 `--verbose`를 명령 앞에 붙이든 뒤에 붙이든 되게 했습니다.
>
> 하나만 강조하고 싶은 게 종료 코드입니다. 성공 0, 실패 1은 당연한데 저희는 **2를 따로 뒀습니다.**
> `analyze`로 100건 돌리다 50건만 성공한 걸 "성공"이라고 하면 자동화에서 거짓말이 되고,
> "실패"라고 하면 성공한 50건을 버리게 됩니다. 그래서 일부 실패는 2입니다.
> `analyze`와 검수 명령 두 개에 붙어 있고, 셸에서 `&&`로 이어 붙이거나 CI에서 이걸로 구분할 수 있습니다.

---

### S6. 4.2 수집 · 4.3 정제 · **세인** · 1:50

**화면**

```
CSV (utf-8-sig)  →  필수 5컬럼 확인  →  source/raw/reviews.jsonl  (원본 그대로)
                                            ↓
                    정제 5규칙        →  source/clean/reviews.jsonl
                                            ↓
                                        SQLite reviews

읽기 단계   필수 5컬럼 헤더 확인 (rating · review_text · review_date · product_name · skin_type)
정제 규칙   ① 본문 있음   ② 공백 정규화   ③ 별점 1~5
            ④ 날짜 통일 (- / .  →  YYYY-MM-DD)   ⑤ 5자 미만 컷
            + 파일 안 중복 제거 (제품·본문·날짜)
```

```text
========================================
          데이터 정제 결과
========================================
정제 대상: 100건
정제 완료: 99건
유효하지 않은 데이터: 0건
중복 제거: 1건
========================================
```

> 제가 맡은 건 파일을 읽어서 쓸 수 있는 데이터로 만드는 부분입니다. `source/` 폴더고, 파일 두 개입니다.
>
> 먼저 읽기입니다. CSV를 `utf-8-sig`로 엽니다. 그냥 `utf-8`로 열면 엑셀에서 저장한 파일의 첫 컬럼 이름 앞에
> 눈에 안 보이는 BOM 문자가 붙어서 `rating`이 `﻿rating`이 됩니다. 그러면 필수 컬럼 검사가 통째로 실패합니다.
> 실제로 겪었고, `utf-8-sig`로 바꿔서 해결했습니다.
>
> 읽은 다음에는 **정제하기 전 원본을 그대로 JSONL로 한 번 저장합니다.** 이게 중요한데,
> 나중에 정제 규칙을 바꿔서 다시 돌리고 싶을 때 원본 CSV가 없어도 됩니다.
> 저희 CLI에 `clean` 명령이 따로 있는 게 이것 때문입니다.
>
> 정제 규칙은 다섯 가지입니다. 필수 필드 검증, 공백 정규화, 별점 1~5 범위 검증,
> 날짜 형식 통일, 그리고 5자 미만 짧은 리뷰 걸러내기입니다.
>
> 날짜가 제일 손이 많이 갔습니다. 같은 데이터 안에 `2025-01-30`, `2025/01/30`, `2025.01.30`
> 세 가지가 섞여 들어옵니다. 이걸 `YYYY-MM-DD` 하나로 통일합니다. 이게 안 되면 뒤에서 월별 추이 차트가 안 그려집니다.
>
> 마지막이 중복 제거입니다. 제품명·본문·날짜 세 개가 같으면 같은 리뷰로 봅니다.
> 실제 데이터 100건 중에 1건이 중복이었고, 그래서 99건이 남았습니다. 화면 아래가 실제 출력입니다.

**포인트** — 세인님 파트는 **"왜 utf-8-sig인가"** 와 **"왜 raw를 따로 남기나"** 두 개만 확실히 말하면 충분합니다. 나머지는 질문 나오면 답하세요.

**질문 대비 — "중복 판정이 두 군데 아닌가요?"** 맞습니다. 그리고 둘의 역할이 다릅니다.
> 세인님 쪽은 **한 파일 안에서** 같은 행이 두 번 들어온 걸 거릅니다. 파이썬 튜플 비교라 빠릅니다.
> 영휘님 DB 쪽은 **이미 저장된 것과 새로 들어온 것**을 비교합니다. 공백을 정규화한 SHA-256 해시를
> `UNIQUE` 컬럼으로 걸어서, 여러 번 import 해도 안 쌓이게 합니다. 화면의 "1건 중복"은 앞쪽,
> "99건 스킵"은 뒤쪽입니다.

**질문 대비 — "날짜가 없으면요?"** 지금은 그 리뷰가 통째로 버려집니다. 계약상 날짜는 없어도 되는 값이라
이건 저희가 인지하고 있는 개선 항목입니다 (S18). 지금 데이터는 100건 전부 날짜가 정상이라 안 드러납니다.

---

### S7. 4.4 AI 감정 분석 · **민규** · 1:30

**화면**

```
analyze_reviews([{id, review_text}])
    → {"results": [{id, sentiment, confidence}], "failed_ids": [int]}

  · 여러 건을 한 프롬프트에 묶어서 호출  (100건 = 100번 호출이 아님)
  · 실패하면 배치를 반으로 쪼개 재시도
  · 넣은 id 는 results 아니면 failed_ids 중 한쪽에 반드시 남는다

  · 프롬프트에 별점을 넣지 않는다  ← 의도적
```

> 감정 분석은 제가 맡았습니다. Gemini를 부르고 결과를 검증하는 부분입니다.
>
> 먼저 호출 방식입니다. 리뷰 한 건에 한 번씩 부르면 100건이면 100번인데, 느리고 비쌉니다.
> 그래서 여러 건을 JSON 배열로 묶어서 한 번에 보냅니다. 대신 모델이 id를 섞거나 빠뜨릴 위험이 생기니까
> 프롬프트에 "입력 id를 결과에 그대로 유지하라, 각 id에 정확히 하나씩 반환하라"를 명시했습니다.
>
> 그리고 실패 처리입니다. 배치로 보내면 한 번 실패할 때 그 배치가 통째로 날아갑니다.
> 그래서 실패하면 **배치를 반으로 쪼개서 다시 시도**합니다. 그래도 안 되면 그 id들을 `failed_ids`에 담아 돌려줍니다.
>
> 계약에서 제일 중요하게 잡은 게 이겁니다. **넣은 id는 반드시 둘 중 한쪽에 나온다.**
> 성공했으면 `results`에, 실패했으면 `failed_ids`에. 둘 다에 없으면 그 리뷰는 조용히 사라진 거고 아무도 모릅니다.
>
> 마지막 줄이 저희가 일부러 한 겁니다. **별점을 프롬프트에 안 넣습니다.**
> 별점 1점이라고 알려주면 모델이 당연히 부정이라고 합니다. 그러면 나중에 계산하는
> '별점-감정 일치도'가 항상 100%가 나오고, 지표가 아무 의미가 없어집니다.
> 그래서 본문만 보고 판단하게 했고, 실측 일치도가 81.8%로 나왔습니다. 이게 진짜 숫자입니다.

**예상 질문 대비** — "503 에러 같은 건 어떻게 처리하나요?"
> 실제로 99건을 한 번에 돌리다가 Gemini가 503 과부하를 냈습니다. 재시도 로직 덕분에 앞의 50건은 저장됐고,
> 뒤 49건은 `failed_ids`로 돌아왔습니다. `analyze --unanalyzed --limit 25`로 나눠서 다시 돌려 복구했습니다.
> 지금 재시도 사이에 대기 시간이 없는 건 저희가 인지하고 있는 개선 항목입니다. (S18에서 다시 언급)

---

### S8. 4.5 키워드·요약 추출 · **민규** · 1:00

**화면**

```
extract_insights([리뷰 본문들])
    → { positive_keywords, negative_keywords, summary, improvements }
    → 모델 호출이 2회 실패하면 예외 대신 None

실측 결과
  긍정 키워드   보습력 · 은은한 향 · 빠른 흡수 · 메이크업 밀림 적음 · 저자극 · 촉촉함
  부정 키워드   무거운 사용감 · 트러블 발생 · 유분 증가 · 향 호불호 · 느린 흡수
  개선 제안     ① 오일프리/라이트 제형 라인 추가
                ② 논코메도제닉 테스트 강화 + 무향 옵션
```

> 두 번째는 키워드와 요약 추출입니다. 명세 4.5는 추출 항목 두 개 이상을 요구하는데,
> 저희는 네 개를 뽑습니다. 긍정 키워드, 부정 키워드, 전체 요약, 개선 제안입니다.
>
> 여기서 설계 결정이 하나 있습니다. **모델 호출이 두 번 다 실패하면 예외를 던지지 않고 `None`을 돌려줍니다.**
> 인사이트는 리포트의 한 섹션일 뿐인데, 이거 하나 실패했다고 통계랑 차트까지 못 보게 되면 안 되니까요.
> 실제로 `None`이 와도 리포트가 정상 생성되는지 확인하는 테스트가 있습니다.
> (입력 자체가 잘못된 경우는 `None`이 아니라 예외입니다. 부르는 쪽 버그니까 조용히 넘기면 안 되죠.)
>
> 아래가 실제로 저희 데이터에서 나온 결과입니다. 개선 제안까지 문장으로 나옵니다.
> 그런데 여기서 문제가 하나 생겼는데, 그게 다음 슬라이드로 이어집니다.

**포인트** — 마지막 줄이 S10의 '개선 우선순위'로 넘어가는 다리입니다. 여기서 끊지 마세요.

---

### S9. 4.6 조회와 검색 · 영휘 · 0:50

**화면**

```bash
python main.py list --sentiment negative --page 1 --size 5
python main.py list --sort rating --page 2
python main.py show 3
python main.py stats --skin-type 지성 --date-from 2025-01-01
```

```text
=== 리뷰 목록 (감정: negative, 1/1 페이지, 총 4건) ===
[ 23] ★★☆☆☆ | 2025-01-30 | 몇 번 사용하니 코 주변에 좁쌀처럼 올라오는 느낌이 있… | negative (0.85)
[ 15] ★★☆☆☆ | 2025-05-06 | 향이 제 취향과 맞지 않고 사용 후 약간 답답한 느낌이… | negative (0.90)
[ 87] ★★☆☆☆ | 2025-05-16 | 기대했던 것보다 보습 지속력이 짧아서 아쉬웠어요. 지성… | negative (0.90)
[ 13] ★☆☆☆☆ | 2025-06-10 | 사용 후 얼굴에 작은 뾰루지가 몇 개 올라와서 사용을 … | negative (0.85)
```

> 조회는 세 명령입니다. `list`가 목록, `show`가 상세, `stats`가 통계입니다.
>
> `list`는 감정·별점·기간으로 필터하고, 정렬하고, 페이지네이션이 됩니다.
> 필터 조건은 SQL의 `WHERE` 절을 조립해서 만듭니다. 파이썬 리스트로 다 가져와서 거르지 않습니다.
> 데이터가 늘어나도 DB가 거르는 게 맞으니까요.
>
> 그리고 필터는 `list`뿐 아니라 `stats`와 `dashboard`에도 똑같이 붙습니다.
> 그래서 "지성 피부 고객만" 또는 "선크림만" 뽑은 대시보드를 그대로 만들 수 있습니다.

---

### S10. 4.7 대시보드 시각화 · 영휘 · 1:30

**화면**

```
차트 7종 (요구 3종)                        답하는 질문
  kpi_summary.png              그래서 몇 건이고 얼마나 부정적인가
  sentiment_distribution.png   감정이 어떻게 나뉘는가
  sentiment_trend.png          시간에 따라 나빠지는가   ← 요구 ②
  rating_distribution.png      별점이 어떻게 분포하는가
  rating_sentiment.png         별점과 감정이 맞는가     ← 요구 ③
  product_sentiment.png        어느 제품이 문제인가
  skin_type_sentiment.png      어느 피부타입에서 갈리는가
                               (감정 분포 = 요구 ①)
```

```text
[개선 우선순위] 부정 키워드 영향도
1. 유분 증가 (우선순위 0.55) — 8건, 부정 25%, 평균 별점 2.75 (-0.90)
2. 느린 흡수 (우선순위 0.32) — 6건, 부정  0%, 평균 별점 3.17 (-0.48)
3. 무거운 사용감 (우선순위 0.21) — 4건, 부정 0%, 평균 별점 3.50 (-0.15)
4. 트러블 발생 — 본문에서 찾지 못했습니다 (모델이 개념으로 요약한 표현. 순위 산정 제외)
```

> 시각화는 제 파트입니다. 명세는 세 종류를 요구하는데 일곱 장을 만들었습니다.
> 세 개는 요구한 것 그대로고, 나머지 넷은 저희가 필요해서 추가한 겁니다.
>
> 세 가지만 말씀드리겠습니다.
>
> **첫째, 색을 검증기로 골랐습니다.** 처음에는 긍정 초록, 부정 빨강으로 했는데,
> 적록색약 시뮬레이션을 돌려보니 초록과 회색이 색 차이 ΔE 1.2로 사실상 구분이 안 됐습니다.
> 그래서 파랑 ↔ 빨강 diverging 팔레트로 바꿨습니다. 지금은 색약에서도 세 감정이 구분됩니다.
>
> **둘째, 한글 폰트가 없으면 죽지 않고 영문으로 떨어집니다.** 리눅스에서 한글 폰트가 없으면
> 차트 글자가 전부 두부처럼 네모(□)로 나옵니다. 폰트를 찾는 함수가 성공 여부까지 같이 돌려주고,
> 실패하면 라벨을 영문으로 바꿔서 그립니다. 차트가 안 나오는 것보다는 영어라도 나오는 게 낫습니다.
>
> **셋째, 이게 오늘 제일 말씀드리고 싶은 건데,** 아래쪽 '개선 우선순위'입니다.
> 민규님이 뽑아준 부정 키워드가 다섯 개인데, 그중에 **뭐부터 고쳐야 하는지**는 안 나옵니다.
> 그래서 제가 각 키워드가 붙은 리뷰를 실제로 세서, 빈도 40% · 부정 비율 40% · 별점 끌어내림 20%로
> 가중해서 점수를 매겼습니다.
>
> 1위가 '유분 증가'인데, 8건에 붙어 있고, 그 8건의 평균 별점이 2.75점입니다. 전체 평균이 3.65니까 0.9점을 끌어내리고 있습니다.
>
> 그리고 4번 5번을 보세요. **"본문에서 찾지 못했습니다"라고 정직하게 씁니다.**
> 모델이 '트러블 발생'이라고 요약했는데, 실제 리뷰 본문에는 "뾰루지가 올라와서"라고 적혀 있지 그 단어가 그대로 있진 않거든요.
> 못 찾은 걸 0건으로 처리해서 순위 맨 아래에 두면, 안 중요한 것처럼 보입니다.
> 그래서 순위에서 빼고 이유를 같이 적었습니다.

**포인트** — 심사자가 "요구는 3종인데 왜 7종?"이라고 물으면 → "3종은 명세 그대로, 4종은 실제로 보다가 필요해서"라고만 답하면 됩니다.

---

### S11. 4.8 리포트 생성 · **민규** · 1:00

**화면**

```
generate_markdown_report(stats, insights, chart_paths, output_path) -> str

  품질 지표 (요구 2개 이상)   별점-감정 일치도 81.8% · 데이터 완전성 100% · 평균 리뷰 길이 44.4자
  TOP N 집계 (요구 1개 이상)  제품별 · 피부타입별 · 낮은 별점 리뷰
  AI 추출 결과               긍정/부정 키워드 · 요약 · 개선 제안
  차트 7장                   상대경로로 삽입  →  GitHub 에서도 보임

  → chart/output/report_20260814_095935.md  (1,493자)
```

> 리포트도 제가 맡았습니다. 통계와 인사이트와 차트 경로를 받아서 마크다운 한 장으로 씁니다.
>
> 명세는 품질 지표 2개 이상, TOP N 1개 이상을 요구하는데 각각 3개씩 넣었습니다.
>
> 하나 짚고 싶은 게 차트 경로입니다. 절대경로를 그대로 박으면 만든 사람 PC에서만 이미지가 보이고
> GitHub에 올리면 다 깨집니다. 그래서 영휘님이 **리포트 파일 기준 상대경로**로 바꿔서 넘겨줍니다.
>
> 그리고 인사이트가 `None`이어도 리포트는 나옵니다. 그 섹션만 빠지고 나머지는 그대로 나옵니다.

**포인트** — 자가 점검에서 리포트에 `[TODO]` 3건이 걸려 있습니다. **먼저 말하는 게 낫습니다.** S18에서 다룹니다.

---

### S12. 4.9 내보내기 · 영휘 · 0:30

**화면**

```bash
python main.py export --format xlsx --rating-min 4
# [INFO] 59건 내보내기 완료: chart/output/reviews_export.xlsx

지원 포맷   csv · jsonl · xlsx   (요구 2개 이상)
필터        --sentiment  --rating-min  --product  --date-from/to
```

> 내보내기는 세 포맷을 지원합니다. 명세는 두 개 이상인데 셋 다 넣었습니다.
> 감정·최소 별점·제품·기간으로 걸러서 뽑을 수 있고, 실제로 별점 4점 이상만 뽑으면 99건 중 59건이 나옵니다.
>
> `openpyxl`이 안 깔려 있으면 죽지 않고 CSV로 떨어뜨립니다. 그리고 **실제로 저장된 경로**를 출력합니다.
> 요청한 xlsx 경로를 그대로 찍으면 없는 파일을 안내하게 되니까요.

---

### S13. 4.10~4.13 설정·로깅·저장·구조·샘플 · 영휘 · 1:00

**화면**

| 명세 | 구현 |
|---|---|
| 4.10 설정 | `config.json` — 중복 정책 · 모델 · 차트 DPI · **경고 임계치 8개** |
| 4.10 로깅 | `logging` INFO/WARNING/ERROR, 콘솔 + `chart/logs/app.log` |
| 4.11 저장 | SQLite 5테이블 (`raw_reviews` `reviews` `analyses` `extractions` `human_labels`) |
| 4.12 구조 | 파이썬 19파일 · 9,199줄 · `chart/modules/` 10개 모듈 |
| 4.13 샘플 | `source/input/cosmetics_reviews_100.csv` **100건** (요구 30건 이상) |

```json
"alerts": {
  "negative_ratio_warn": 0.15,   "negative_ratio_critical": 0.30,
  "spike_delta": 0.10,           "min_bucket_size": 5,
  "group_negative_ratio_warn": 0.30, "min_group_size": 5,
  "agreement_warn": 0.60,        "confidence_warn": 0.65
}
```

> 여기는 빠르게 넘기겠습니다. 설정 파일, 로깅, SQLite, 모듈 분리, 샘플 데이터 — 명세 요구는 다 충족합니다.
>
> 한 가지만 짚으면 `config.json`의 `alerts` 블록입니다. 경고를 낼 기준선 여덟 개가 여기 들어 있습니다.
> 처음에는 코드 안에 0.15, 0.30 같은 숫자가 그냥 박혀 있었는데, 그러면 화장품이 아니라 다른 제품군에 쓸 때
> 코드를 고쳐야 합니다. 그래서 전부 설정으로 뺐고, **비율 임계치 여섯 개는 0~1 범위인지**,
> 심각선이 경고선보다 큰지 검증합니다. (나머지 둘은 표본 개수라 비율이 아닙니다.)
>
> 그리고 이 임계치는 **업계 표준이 아니라 이 데이터셋 기준의 출발점**입니다. 문서에도 그렇게 적어놨습니다.
>
> 로그는 파일로도 남습니다. 경고가 판정되는 순간 값과 기준을 같이 찍어서, 회차별로 언제부터 나빠졌는지 추적할 수 있게 했습니다.

---

## 5. 보너스 과제

### S14. 보너스 · 영휘 · 1:00

**화면**

| 보너스 | 상태 | 내용 |
|---|---|---|
| 감정 변화 알림 | ✅ | 경고 5종 · 임계치 설정화 · **확인 후보/다음 지표 분리** |
| 제품·카테고리 비교 | ✅ | 차트 2종 + `--product`/`--skin-type` 필터 + 그룹별 부정 비율 경고 |
| 다국어 감정 분석 | △ | `language` 자동 판별(ko/en) · 프롬프트 언어 비의존. **영어 실데이터 미검증** |
| HTML 대시보드 | ❌ | 미구현. PNG 7장 + Markdown 리포트로 대체 |

```text
[! ] 부정 비율이 직전 월 대비 급증했습니다 (2025-05)
     2025-04 0% → 2025-05 14% (+14%p, 표본 14건)
     · 확인 후보: 제품 '수분 장벽 크림' 에 몰려 있습니다 — 이 구간 부정의 50% (전체 평균 25%)
     · 확인 후보: 피부타입 '건성' 에 몰려 있습니다 — 이 구간 부정의 50% (전체 평균 25%)
     · 다음 지표: 해당 구간의 생산 로트·유통 경로
     · 다음 지표: 직전 리뉴얼·성분 변경 시점
```

> 보너스는 네 개 중 둘을 했고, 하나는 절반, 하나는 못 했습니다. 못 한 것부터 말씀드리면 HTML 대시보드는 안 했습니다.
> 다국어는 언어 판별과 저장은 되고 프롬프트도 언어에 안 묶여 있는데, **영어 리뷰로 실제 검증을 안 했습니다.**
> 그래서 된다고 말씀드리지 않겠습니다.
>
> 대신 감정 변화 알림은 좀 깊게 했습니다. 화면 아래가 실제 출력입니다.
>
> 여기서 저희가 신경 쓴 게 하나 있는데, **경고가 원인을 단정하지 않습니다.**
> "5월에 부정이 늘었다"까지는 데이터가 말할 수 있지만, "왜"는 데이터 밖에 있습니다.
> 그래서 이름을 나눴습니다. 도구가 확인한 건 '확인 후보'로, 도구가 모르는 건 '다음 지표'로 적습니다.
>
> '수분 장벽 크림에 몰려 있다'는 저희가 실제로 센 겁니다. 그런데 '생산 로트가 문제다'는 저희 DB에 없는 정보입니다.
> 이 둘을 같은 톤으로 쓰면, 읽는 사람이 도구를 믿고 엉뚱한 데로 갑니다.
>
> 사실 이 부분이 제일 고민한 지점입니다. AI가 그럴듯한 원인을 지어내게 만드는 건 쉬운데,
> 그러면 도구가 아니라 점쟁이가 되니까요.

**포인트** — **못 한 걸 먼저 말하세요.** 그 다음에 한 걸 말하면 신뢰도가 올라갑니다. 순서 바꾸지 마세요.

---

## 6·7. 개발 환경과 제약 사항

### S15. 개발 환경과 제약 · 영휘 · 0:30

**화면**

```
6. 개발 환경
   Python 3.10+ 요구 → 충족 (코드는 3.9 호환까지 낮춰 둠)
   AI 호출: google-genai 공식 SDK
   의존성 4개: google-genai · python-dotenv · matplotlib · openpyxl
     matplotlib 없으면 → 차트만 건너뜀
     openpyxl  없으면 → export xlsx 가 csv 로 떨어짐 (단, xlsx *입력* 은 불가)

7. 제약 사항
   7.1 실시간 웹 대시보드 없음  → 정적 PNG + 파일 리포트  ✅
   7.1 크롤링 없음              → 파일 입력만              ✅
   7.2 API 키 코드에 없음       → .env + .gitignore        ✅
```

> 환경과 제약은 빠르게 넘기겠습니다. 파이썬 3.10 이상 요구인데 저희 코드는 3.9에서도 돌게 낮춰 뒀습니다.
> 팀원 환경이 다를 수 있으니까요.
>
> API 호출은 구글 공식 SDK를 씁니다. 의존성은 네 개뿐이고, 그중 둘은 없어도 프로그램이 안 죽습니다.
> 차트를 건너뛰거나 xlsx를 csv로 떨어뜨립니다. 다만 **엑셀 파일을 입력으로 넣을 때는 `openpyxl`이 필요합니다.**
> 읽을 방법 자체가 없으니까요. 그건 메시지로 알려줍니다.
>
> 제약 세 가지 다 지켰습니다. API 키는 `.env`에만 있고 `.gitignore`에 들어 있어서 커밋되지 않습니다.

---

## 8. 결과 예시 — 라이브 데모

### S16. 데모 · 3인 · 3:00

> **데모 원칙** — `analyze`와 `extract`는 API를 부르니까 현장에서 돌리지 마세요.
> 네트워크가 흔들리면 발표가 멈춥니다. **이미 분석된 DB 상태에서 시작**하고,
> 8.2만 미리 찍은 화면을 띄우거나 `--limit 3`으로 짧게 보여주세요.

#### 8.1 리뷰 데이터 가져오기 (세인)

```bash
python main.py import --file source/input/cosmetics_reviews_100.csv
```

```text
[INFO] B importer: 100건 읽음 -> source/raw/reviews.jsonl
=== 정제 시작 (B 모듈) ===
정제 대상: 100건 / 정제 완료: 99건 / 중복 제거: 1건
[INFO] clean 저장소 반영: 신규 0건, 갱신 0건, 스킵 99건 (정책=skip)

총 100건 감지, 유효 99건, 저장 0건 (갱신 0건, 중복 스킵 99건)
```

> 같은 파일을 두 번 넣으면 이렇게 됩니다. 100건을 읽고 99건으로 정제했는데, 저장은 0건입니다.
> 전부 이미 있는 리뷰라 중복 정책 `skip`이 걸린 거죠. **두 번 돌려도 데이터가 두 배가 되지 않습니다.**

#### 8.2 AI 감정 분석 (민규)

```bash
python main.py analyze --unanalyzed --limit 25
```

```text
[INFO] 분석 대상: 25건
분석 완료: 25건 저장, 0건 실패
```

> `--unanalyzed`는 아직 분석 안 된 것만 고릅니다. 이미 분석된 건 기본으로 건너뛰고,
> 다시 하고 싶으면 `--force`를 붙입니다. 실패한 게 있으면 실패한 ID를 그대로 찍어줍니다.

#### 8.3 목록 조회 (영휘)

```bash
python main.py list --sentiment negative --page 1 --size 5
```
> (S9 화면과 동일)

#### 8.4 통계 요약 ★ 핵심 (영휘)

```bash
python main.py stats
```

```text
[WARNING] [negative_spike] 부정 비율이 직전 월 대비 급증했습니다 (2025-05) ... | 값 0.1429 / 기준 0.1000
[INFO] 임계치 판정: 심각 0 · 경고 1 · 안내 0

=== 리뷰 분석 통계 ===
총 리뷰 수: 99건        분석 완료: 99건 (100.0%)

[감정 분포]  긍정 57건 (57.6%) · 중립 38건 (38.4%) · 부정 4건 (4.0%)
[별점 분포]  ★5 33 · ★4 26 · ★3 20 · ★2 12 · ★1 8

평균 별점 3.65    평균 확신도 0.91
별점-감정 일치도 81.8%    데이터 완전성 100.0%

[개선 우선순위] 1. 유분 증가 (0.55) — 8건, 부정 25%, 평균 별점 2.75 (-0.90)
[경고 · 안내] 1건
```

> 이게 오늘 제일 보여드리고 싶은 화면입니다. 명령어 하나에 다 나옵니다.
>
> 위에 경고가 먼저 뜨고 — 5월에 부정이 튀었다 — 그 다음 통계, 그 다음 뭐부터 고칠지가 나옵니다.
>
> 여기 **별점-감정 일치도 81.8%**를 봐주세요. 100%가 아닌 게 정상입니다.
> 아까 민규님이 말씀하신 대로 프롬프트에 별점을 안 넣었으니까, 이건 AI가 본문만 보고 판단한 결과와
> 고객이 실제로 준 별점이 얼마나 맞는지를 재는 겁니다. 100%가 나온다면 오히려 뭔가 잘못된 거고요.

#### 8.5 키워드 추출 (민규)

```bash
python main.py extract
```
> (S8 화면과 동일 — 실행하지 말고 화면만)

#### 8.6 대시보드 리포트 (영휘)

```bash
python main.py dashboard
```

```text
[생성된 차트] 7/7장
  - chart/output/kpi_summary.png
  - chart/output/sentiment_distribution.png
  ... (7장)

[리포트] chart/output/report_20260814_095935.md (1,493자)
```

> 마지막입니다. 차트 일곱 장을 그리고 리포트를 씁니다. **몇 장 중 몇 장인지 찍습니다.**
> 데이터가 없어서 못 그린 장이 있으면 어느 장이 빠졌는지 이름까지 나옵니다.
>
> 리포트 파일을 열어서 보여드리면 — 통계, 품질 지표, TOP N, AI 인사이트, 그리고 차트 일곱 장이
> 마크다운 안에 다 들어가 있습니다.

**데모 실패 대비** — DB가 꼬이면 `python main.py status` 한 줄로 지금 상태를 보여주고 넘어가세요.

---

## S17. 우리는 이걸 어떻게 믿나 · 영휘 · 1:00

**화면**

```bash
python chart/check_contract.py           # 계약 자가 점검
python chart/tests/test_integration.py   # 통합 테스트
python main.py review score              # AI 정확도 (사람 라벨 대비)
```

```text
Ran 58 tests in 3.092s        OK (skipped=2)

통과 14 · 실패 1 · 미구현 0 · 요청 3

=== AI 검수 결과 ===
AI-사람 일치율   : 100.0%  (기준 80%) [OK]
검수자 간 일치율 :  75.0%  (기준 85%) [미달]
  → AI 보다 라벨 기준이 먼저입니다. 사람끼리 갈리는데 AI 만 고쳐봐야 소용없습니다.
```

> 마지막으로 "이게 맞다는 걸 어떻게 아느냐"입니다. 세 가지로 확인합니다.
>
> 첫째, **계약 자가 점검.** 세 명이 주고받는 함수는 다섯 개뿐인데, 그 다섯 개가 약속대로 동작하는지
> 실제로 불러서 확인하는 스크립트입니다. 문서에 적어두면 아무도 안 읽으니까 실행되게 만들었습니다.
>
> 둘째, **통합 테스트 58개.** 3초 만에 끝나고 **API 키가 없어도 돕니다.**
> 테스트 한 번 돌릴 때마다 과금되면 아무도 테스트를 안 돌리게 되니까요.
>
> 셋째가 좀 다른 건데, **AI가 얼마나 맞는지를 사람 라벨로 쟀습니다.**
> 표본을 뽑아서 CSV로 내보내고, 두 사람이 각자 긍정/중립/부정을 매기고, 다시 넣어서 일치율을 계산합니다.
>
> 결과가 재밌었습니다. AI와 사람의 일치율은 100%인데, **사람끼리의 일치율이 75%로 기준 미달**이었습니다.
> 같은 리뷰를 보고 한 명은 중립, 한 명은 부정이라고 한 게 5건 있었습니다.
>
> 이건 AI 문제가 아니라 **저희 라벨 기준이 없다는 뜻**입니다. 사람끼리 갈리는데 프롬프트만 고쳐봐야 소용없죠.
> 도구가 그걸 그대로 출력하게 해뒀습니다.

**포인트** — 이 슬라이드가 발표의 승부처입니다. "테스트 있어요"가 아니라 **"불편한 숫자도 그대로 낸다"**가 메시지입니다.

---

## S18. 남은 것과 배운 것 · 3인 · 1:00

**화면**

```
남은 것  (알고 있고, 어디를 고쳐야 하는지도 압니다)
  · extractor 검증기가 계약에 없는 조건을 요구 → [FAIL] 1건, 고칠 위치 확정
  · 리포트가 차트 이름 대신 '차트 1~7' 번호를 매김            [TODO]
  · 리포트가 생성 시각·필터 범위를 안 읽음                    [TODO]
  · B 영역 자동 테스트 없음 · Gemini 재시도 사이 대기 없음
  · 날짜가 없는 리뷰는 통째로 버려짐 (계약상 nullable인데)
  · list 총 건수가 COUNT(*) 가 아니라 행을 다 세는 방식
  · HTML 대시보드 미구현 · 다국어 실데이터 미검증

배운 것
  영휘 — 숫자는 한 곳에서만 세야 한다. 두 곳에서 세면 언젠가 다르다
  세인 — utf-8-sig 한 글자 차이로 전체가 멈춘다. 원본은 무조건 남긴다
  민규 — 배치 호출은 빠른 대신, id 를 잃지 않는 계약이 먼저다
```

> 마지막입니다. 남은 것부터 말씀드리겠습니다.
>
> 자가 점검에 `[FAIL]`이 1건, `[TODO]`가 3건 떠 있습니다. 저희가 숨기지 않고 그대로 두고 있는데,
> 이유가 있습니다. **`[FAIL]`은 "만들었는데 약속과 다름"이고, `[TODO]`는 "새로 부탁하는 것"입니다.**
> 이걸 섞으면, 다 만든 사람도 빨간불이 뜨고, 그러면 진짜 `[FAIL]`을 보고도 아무도 안 움직이게 됩니다.
>
> 그 외에도 B 영역에 자동 테스트가 없고, Gemini 재시도 사이에 대기가 없고,
> 날짜가 없는 리뷰가 통째로 버려지는 문제를 알고 있습니다. 지금 데이터는 100건 전부 날짜가 정상이라 안 드러날 뿐입니다.
>
> 배운 걸 한 줄씩 말씀드리고 마치겠습니다.
>
> (영휘) 저는 숫자를 두 곳에서 세면 언젠가 반드시 달라진다는 걸 배웠습니다.
> 그래서 집계 함수를 하나로 몰았고, 그게 이 프로젝트에서 제일 잘한 결정이었다고 생각합니다.
>
> (세인) 저는 `utf-8`과 `utf-8-sig` 한 글자 차이로 전체가 멈추는 걸 겪었습니다. 그리고 원본은 무조건 남겨야 한다는 것도요.
>
> (민규) 저는 배치로 묶어 부르면 빠른데, 그만큼 id를 잃지 않는 계약이 먼저 있어야 한다는 걸 배웠습니다.
>
> 감사합니다.

---

## 부록 A. 예상 질문과 답변

| 질문 | 답변 요지 |
|---|---|
| **왜 CLI인가요? 웹이 더 낫지 않나요?** | 명세 7.1이 실시간 웹 대시보드를 제외했습니다. 그리고 CLI는 종료 코드로 자동화에 그대로 물립니다 — 매일 새벽에 돌려 경고가 뜨면 알림 보내는 게 웹보다 쉽습니다. |
| **별점을 프롬프트에 넣으면 정확도가 오를 텐데요?** | 오릅니다. 대신 '별점-감정 일치도'라는 지표가 죽습니다. 100%가 보장되니까요. 저희는 정확도보다 **측정 가능성**을 택했습니다. |
| **일치도 81.8%면 낮은 거 아닌가요?** | 나머지 18.2%가 저희가 볼 지점입니다. 실제 사례가 리포트에 있습니다 — 별점 **1점**인데 본문은 *"좁쌀처럼 올라오는 느낌이 있었어요. **건성이라 여러 번 덧발라도 부담이 크지 않았어요**"* 라서 AI가 중립으로 봤습니다. 본문만 보면 중립이 맞습니다. 별점과 본문이 실제로 어긋난 리뷰를 찾아내는 게 이 지표의 목적입니다. |
| **API 키 없이 어디까지 되나요?** | 12개 중 10개가 됩니다. `analyze`와 `extract`만 키가 필요합니다. 테스트 58개와 계약 점검 전체도 키 없이 돕니다. |
| **차트가 7장인데 명세는 3장 아닌가요?** | 3장은 명세 그대로 들어 있습니다. 나머지 4장은 저희가 데이터를 보다가 필요해서 추가했습니다. 특히 제품별·피부타입별은 보너스의 '카테고리 비교'에 해당합니다. |
| **왜 폴더를 나눴나요? 그냥 같이 짜면 안 되나요?** | 3인이 같은 파일을 건드리면 머지 충돌에 시간을 다 씁니다. 폴더를 나누고 접점을 함수 5개로 줄였더니, 각자 상대 코드 없이 개발할 수 있었습니다. |
| **`[FAIL]`이 남아 있는데 미완성 아닌가요?** | 남은 1건은 위치와 원인이 확정돼 있습니다. `extractor`의 검증기가 계약에 없는 조건(개선 제안 2개 이상)을 요구해서, 모델이 1개만 준 날 통째로 실패합니다. 계약을 고치거나 검증기를 맞추면 끝입니다. |
| **테스트 58개 중 2개가 skip인데요?** | 실제 Gemini를 부르는 테스트 2개입니다. 키가 있으면 돌고, 없으면 건너뜁니다. 테스트가 과금되면 아무도 안 돌립니다. |
| **검수자 간 일치율 75%는 실패 아닌가요?** | 실패 맞습니다. 그리고 그게 정확히 저희가 알아야 했던 겁니다. AI를 고치기 전에 라벨 기준부터 만들어야 한다는 뜻이니까요. 도구가 그 판단을 대신 해주진 않지만, 숫자로는 보여줍니다. |
| **데이터가 100건뿐인데 늘어나면?** | 필터와 페이지네이션은 SQL `WHERE`/`LIMIT`으로 처리하고, 감정 분석은 배치로 묶어 부릅니다. 다만 두 곳이 아직 건수에 묶여 있습니다 — `list`의 **총 건수 계산이 `COUNT(*)`가 아니라 행을 다 세는 방식**이고, `extract`는 프롬프트에 넣을 리뷰를 60건으로 자릅니다. 앞은 `COUNT(*)` 한 줄이면 되고, 뒤는 표본 추출 전략이 필요합니다. |
| **개선 우선순위의 '빈도 40%'는 절대 건수인가요?** | 아니요, **가장 많이 걸린 키워드 대비 상대값**입니다. 8건이 최다면 그게 1.0이고 4건은 0.5입니다. 별점 끌어내림도 4점 만점 기준으로 정규화합니다. 절대 건수를 쓰면 데이터가 커질 때 점수가 1을 넘어가서 세 항목의 가중치가 의미를 잃습니다. |
| **`.tables` 하면 테이블이 6개인데요?** | 5개가 저희 것이고, `sqlite_sequence`는 `AUTOINCREMENT`를 쓰면 SQLite가 자동으로 만드는 내부 테이블입니다. |

---

## 부록 B. 발표 전 점검표

```bash
# 발표 30분 전, 이 순서로 한 번 돌려보세요. 전부 API 키 없이 됩니다.

python main.py status                     # DB에 99건 · 분석 99건 있는지
python chart/tests/test_integration.py    # 58 OK 나오는지
python chart/check_contract.py            # 통과 14 · 실패 1 · 요청 3
python main.py stats                      # 경고 1건 · 우선순위 1위 '유분 증가'
python main.py dashboard                  # 7/7장
```

- [ ] 차트 PNG 7장이 `chart/output/`에 최신으로 있는가 (한글이 □로 안 나오는가)
- [ ] 리포트 `.md` 파일을 열어둘 창을 미리 준비했는가
- [ ] `analyze` / `extract`는 **실행하지 않고 화면만** 띄우기로 합의했는가
- [ ] 터미널 글자 크기를 뒷자리에서 보이게 키웠는가
- [ ] `.env`가 화면에 안 뜨게 터미널을 정리했는가

---

## 부록 C. 명세 → 슬라이드 대응표 (심사자 배포용)

| 명세 | 요구 | 구현 | 슬라이드 |
|---|---|---|---|
| 4.1 | 서브커맨드 10개 | 12개 (필수 10 + `review` `status`) | S5 |
| 4.2 | CSV/Excel 수집 · raw 저장 | `utf-8-sig` CSV + Excel→임시CSV · `raw_reviews` | S6 |
| 4.3 | 정제 5규칙 · 중복 정책 | 5규칙 전부 + `skip`/`upsert` · SHA-256 해시 | S6 |
| 4.4 | 감정 3종 + 확신도 · 3옵션 | 충족 · 배치 호출 · `failed_ids` 계약 | S7 |
| 4.5 | 추출 2개 이상 | 4개 (긍정·부정 키워드 · 요약 · 개선 제안) | S8 |
| 4.6 | list / show / stats | 충족 · 필터가 `stats`·`dashboard`에도 적용 | S9 |
| 4.7 | 차트 3종 · 한글 · PNG | **7종** · 폰트 폴백 · 색약 검증 팔레트 | S10 |
| 4.8 | 품질 2 · TOP N 1 · AI 결과 | 품질 3 · TOP N 3 · AI 4항목 · MD 저장 | S11 |
| 4.9 | 포맷 2개 이상 | **3개** (csv · jsonl · xlsx) | S12 |
| 4.10 | config.json · logging | 충족 + **경고 임계치 8개 설정화** | S13 |
| 4.11 | 영구 저장소 | SQLite 5테이블 | S13 |
| 4.12 | 모듈 4개 이상 | **10개** (`chart/modules/`) | S13 |
| 4.13 | 샘플 30건 이상 | **100건** CSV | S13 |
| 5 | 보너스 4종 | 2 완료 · 1 부분 · 1 미구현 | S14 |
| 6 | Python 3.10+ · 공식 SDK | 충족 (3.9 호환) · `google-genai` | S15 |
| 7 | 웹·크롤링 제외 · 키 분리 | 전부 준수 | S15 |
